import React from 'react';

import { Button } from '@material-ui/core/';

export default TestPageContainer = () => {
    
    return (
        <div>
            TestPageContainer
            <Button>
                Назад
            </Button>
        </div>
    );
}